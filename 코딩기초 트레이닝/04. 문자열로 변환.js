// 내가 해결한 방법

function solution(n) {
  return n.toString();
}

// 다른 사람들의 방법 중 기억에 남는 방법

// 템플릿 리터럴을 사용
function solution(n) {
  return `${n}`;
}

// 인자n과 빈 문자열 더하기
// 문자열과 다른 데이터 타입을 더하면 문자열로 형변환

function solution(n) {
  return n + "";
}
